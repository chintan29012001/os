#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include"ls.h"

int main(int argc, char const *argv[])
{
    /* code */
    while(1)
    {
        char s[100];
        printf("boomerOp>>");
        scanf("%s",s);
        if(strcmp(s,"exit")==0)
        {
            printf("exiting now.... \n");
            exit(0);
        }
        else 
        {
            char command[50][50];
            

        }
        // printf("%s",s);
        // printf("\n");

        

    }
    return 0;
}
